{
  "name": "DATA_RETENTION_MINIO",
  "type": "MINIO",
  "category": "dataRetention",
  "authenticationType": "access_keys",
  "config": {
    "optionFields": ["bucketName", "prefix", "endPoint", "useSSL"],
    "secretFields": ["accessKeyID", "secretAccessKey"]
  }
}
