{
  "name": "DESTINATION_SALESFORCE_OAUTH",
  "type": "salesforce",
  "category": "destination",
  "authenticationType": "oauth",
  "config": {
    "optionFields": ["environment"],
    "refreshOAuthToken": true
  },
  "displayOptions": {
    "deprecated": true,
    "deprecationLabel": "Deprecated — create a new account using the recommended OAuth option."
  }
}
