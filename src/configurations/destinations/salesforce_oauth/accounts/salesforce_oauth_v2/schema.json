{
  "optionsSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "environment": {
        "type": "string",
        "description": "Salesforce environment to authenticate against (Production or Sandbox).",
        "enum": ["Production", "Sandbox"],
        "default": "Production"
      }
    }
  }
}
