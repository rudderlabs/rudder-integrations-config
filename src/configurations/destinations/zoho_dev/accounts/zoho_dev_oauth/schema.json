{
  "optionsSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "required": ["region"],
    "type": "object",
    "properties": {
      "region": {
        "type": "string",
        "enum": ["US", "EU", "IN"],
        "default": "US"
      }
    }
  }
}
