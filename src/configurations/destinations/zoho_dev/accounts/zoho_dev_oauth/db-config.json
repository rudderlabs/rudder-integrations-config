{
  "name": "DESTINATION_ZOHO_DEV_OAUTH",
  "type": "zoho_dev",
  "category": "destination",
  "authenticationType": "oauth",
  "config": {
    "optionFields": ["region"],
    "oauth": {
      "generateOAuthToken": true,
      "refreshTokenInDataplane": true
    }
  }
}
