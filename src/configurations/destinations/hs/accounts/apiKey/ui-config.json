{
  "uiConfig": [
    {
      "label": "Name Key",
      "description": "Name Key for the Account",
      "type": "string",
      "configkey": "name",
      "required": true
    },
    {
      "label": "API Key",
      "description": "API Key for the HS Destination",
      "type": "string",
      "configkey": "apiKey",
      "required": true
    },
    {
      "label": "Sandbox",
      "description": "Sandbox for the HS Destination",
      "type": "boolean",
      "configkey": "sandbox",
      "required": false
    }
  ]
}
