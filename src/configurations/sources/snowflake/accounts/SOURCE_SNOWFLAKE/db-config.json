{
  "name": "SOURCE_SNOWFLAKE",
  "type": "snowflake",
  "category": "source",
  "authenticationType": "configurable",
  "config": {
    "optionFields": ["account", "dbname", "warehouse", "user", "role", "authenticationType"],
    "secretFields": ["password", "privateKey", "privateKeyPassphrase"]
  }
}
