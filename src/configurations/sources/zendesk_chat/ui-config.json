{
    "uiConfig": {
        "auth": {
            "type": "OAuth",
            "config": [
                {
                    "fields": [
                        {
                            "type": "textInput",
                            "label": "Subdomain",
                            "value": "subdomain",
                            "required": true,
                            "footerNote": "Subdomain of your Zendesk Chat URL",
                            "regex": "^(.{0,100})$",
                            "regexErrorMessage": "Invalid Value"
                        }
                    ]
                }
            ]
        }
    }
}