{
  "uiConfig": [
    {
      "title": "Connection Settings",
      "fields": [
        {
          "type": "checkbox",
          "label": "Put source request details in rudder event context",
          "value": "putRequestDetailsInContext",
          "default": false,
          "footerNote": "If enabled, the source request details (headers, query_parameters, etc.) will be added to the rudder event context"
        }
      ]
    }
  ]
}
