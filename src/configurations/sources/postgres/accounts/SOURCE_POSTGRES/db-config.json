{
  "name": "SOURCE_POSTGRES",
  "type": "postgres",
  "category": "source",
  "authenticationType": "password",
  "config": {
    "optionFields": ["host", "dbname", "user", "port", "sslMode"],
    "secretFields": ["password"]
  }
}
