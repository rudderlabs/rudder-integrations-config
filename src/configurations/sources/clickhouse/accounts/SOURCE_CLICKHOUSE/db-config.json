{
  "name": "SOURCE_CLICKHOUSE",
  "type": "clickhouse",
  "category": "source",
  "authenticationType": "password",
  "config": {
    "optionFields": ["host", "port", "database", "user", "secure", "skipVerify", "scratchDatabase"],
    "secretFields": ["password"]
  }
}
