{
  "name": "MCP_MIXPANEL_API_KEY",
  "type": "Mixpanel",
  "category": "mcpIntegration",
  "authenticationType": "api_key",
  "config": {
    "optionFields": ["projectId", "region"],
    "secretFields": ["serviceAccountUsername", "serviceAccountSecret"]
  },
  "displayOptions": {
    "isBeta": true,
    "hidden": {
      "gate": {
        "flags": [
          {
            "name": "enable-mcp-integrations",
            "value": false
          }
        ]
      }
    }
  }
}
