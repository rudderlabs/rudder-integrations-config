{
  "secretSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["apiKey"],
    "properties": {
      "apiKey": {
        "type": "string",
        "pattern": "^(.{1,500})$"
      }
    },
    "additionalProperties": false
  },
  "optionsSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["siteId", "region"],
    "properties": {
      "siteId": {
        "type": "string",
        "pattern": "^(.{1,200})$",
        "description": "Customer.io site ID."
      },
      "region": {
        "type": "string",
        "enum": ["US", "EU"],
        "default": "US",
        "description": "Customer.io region for MCP API requests."
      }
    },
    "additionalProperties": false
  }
}
