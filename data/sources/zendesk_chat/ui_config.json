{
  "uiConfig": {
    "auth": {
      "config": [
        {
          "fields": [
            {
              "footerNote": "Subdomain of your Zendesk Chat URL",
              "label": "Subdomain",
              "regex": "^(.{0,100})$",
              "regexErrorMessage": "Invalid Value",
              "required": true,
              "type": "textInput",
              "value": "subdomain"
            }
          ]
        }
      ],
      "type": "OAuth"
    }
  }
}